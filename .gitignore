# 大文件产物（exe/zip 等通过 GitHub Releases 分发，不入 git）
*.exe
*.exe.bak
*.zip
*.7z
*.rar

# 构建产物
dist/
build/
*.spec.bak

# 缓存与临时文件
__pycache__/
*.pyc
.pytest_cache/
.reasonix/
logs/

# 日志
*.log
# 运行/排查日志（如用户拷回的 Log4.txt，含本机路径与课程地址，不入库）
Log[0-9]*.txt

# 浏览器驱动（体积大，按需自行下载）
edgedriver_win64/

# 备份（含 exe 备份）
backup/

# Python 虚拟环境
venv/
.venv/
# Linux 运行环境（pip 目标安装 / deb 解包 / 驱动二进制，按 run_local.sh 与 README 重建）
学习通刷课/shuake_src/.pylibs/
学习通刷课/shuake_src/.syslib/
学习通刷课/geckodriver/

# Selenium Manager 误下载的 Edge 测试浏览器与驱动（642MB/25MB）
学习通刷课/shuake_src/MicrosoftEdge/
学习通刷课/shuake_src/msedgedriver/

# 运行时用户数据（含登录凭据/账号配置/课程缓存，绝不入库）
学习通刷课/shuake_src/task/tool/cookies.pkl
学习通刷课/shuake_src/task/tool/account_info.json
学习通刷课/shuake_src/task/tool/course_name.json

# 刷课运行时缓存（题目缓存 / 字体混淆样本，运行时自动生成，不入库）
学习通刷课/shuake_src/task/record/cache_*.pkl
学习通刷课/shuake_src/task/record/font_samples/

# Wine 交叉构建环境（Windows Python prefix / 构建中间产物，共 ~3.7GB）
# Wine 交叉构建环境（prefix/home/cache 体积大, 按需重建）
学习通刷课/wine-build/*
!学习通刷课/wine-build/build.sh
知到刷课/autovisor-src/Autovisor-main/wine-build/*
!知到刷课/autovisor-src/Autovisor-main/wine-build/build.sh
!知到刷课/autovisor-src/Autovisor-main/wine-build/AutovisorGUI.spec
台账.txt
.filter-backup/

# deb 打包产物
学习通刷课/deb-build/pkg/
知到刷课/deb-build/pkg/

# 知到运行时依赖（pip 目标安装的 numpy/opencv，可按需重建）
知到刷课/autovisor-src/Autovisor-main/res/
# 本地 playwright 浏览器缓存（PLAYWRIGHT_BROWSERS_PATH 重定向到此，302MB）
知到刷课/autovisor-src/Autovisor-main/.playwright_browsers/
.runtime_libs/
*.deb

# 含登录凭据的运行时数据（账号密码 / 智慧树登录票据, 绝不入库）
# 注意: 上游遗留名 configs.ini 与 data/cookies.json 曾入库, 已随提交 dfbfdc1
# 从版本控制移除; 旧凭据仍留在 git 历史中 —— 请轮换账号密码并重新登录作废旧票据。
知到刷课/autovisor-src/Autovisor-main/configs.ini
知到刷课/autovisor-src/Autovisor-main/config.ini
知到刷课/autovisor-src/Autovisor-main/data/cookies.json
知到刷课/autovisor-src/Autovisor-main/data/exam_cookies.json

# 依赖安装器临时下载的 wheel（应下载到 packages/，此处兜底忽略）
*.whl
